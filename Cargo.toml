[package]
name = "resonate-resume-repro"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
resonate = { version = "0.3.0", package = "resonate-sdk" }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
serde = { version = "1", features = ["derive"] }
